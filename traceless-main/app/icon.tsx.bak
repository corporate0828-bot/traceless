import { ImageResponse } from "next/og";

export const size = {
  width: 128,
  height: 128,
};

export const contentType = "image/png";

export default function Icon() {
  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          background:
            "linear-gradient(135deg, #0b1220 0%, #101b34 55%, #1d4ed8 100%)",
          color: "#ffffff",
          fontSize: 48,
          fontWeight: 800,
          letterSpacing: "-0.08em",
          borderRadius: 28,
        }}
      >
        TL
      </div>
    ),
    size
  );
}